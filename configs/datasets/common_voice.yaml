name: common_voice
hf_dataset: mozilla-foundation/common_voice_16_1

language: en

audio:
  sample_rate: 16000
  max_duration: 2.0
  mono: true

labels:
  type: speaker_id
  min_utterances_per_class: 5
  max_speakers: 100

splits:
  split: train

protocol:
  grouping: speaker
